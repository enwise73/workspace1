# CLAUDE.md — GoNow 프로젝트 작업 규칙

> 이 파일은 Claude / Cursor가 이 저장소에서 작업할 때 **매번 지켜야 하는 규칙**이다. 무엇을 만들지는 `PRD.md`에 있다. 이 파일은 **어떻게 만들지**만 다룬다. 규칙이 PRD와 충돌하면 이 파일이 우선한다. 규칙 자체를 바꿔야 한다고 판단되면, 코드를 고치기 전에 먼저 물어본다.

---

## 1\. 프로젝트 한 줄 요약

운정중앙역 → (GTX-A) → 서울역 → (버스) → 명동 교원빌딩 통근 동선에 대해, **영업일 아침에만** 날씨·준비물과 함께 **"지금 나가라 / N분 뒤 나가라"** 하나의 결론을 주는 개인용 PWA. GitHub Pages에 정적 배포, 텔레그램 봇으로 알림, 월 운영비 0원.

---

## 2\. 절대 규칙 (위반 시 되돌린다)

| \# | 규칙 |
| :---- | :---- |
| A1 | **프런트엔드는 Next.js(App Router, JavaScript) 정적 export만 쓴다.** `next.config.mjs`의 `output: 'export'`를 유지한다. 서버 사이드 렌더링·API 라우트·서버 전용 런타임 기능은 쓰지 않는다 — 배포 대상이 GitHub Pages(정적 파일 호스팅)이기 때문이다. `next build`가 만든 정적 산출물 그대로 올라간다. TypeScript는 도입하지 않는다(순수 JS 유지). |
| A2 | **프런트엔드 의존성은 `next`/`react`/`react-dom`/`eslint`(+`eslint-config-next`)로 제한한다.** day.js·axios 등 표준 API(`fetch`, `Intl`, `Date`)로 대체 가능한 라이브러리는 추가하지 않는다. 이 목록 밖의 새 npm 의존성은 프런트·Actions 스크립트를 불문하고 추가 전에 반드시 물어본다. |
| A3 | **API 키·토큰을 저장소에 절대 커밋하지 않는다.** 코드·주석·예제·테스트 픽스처 어디에도 금지. 키는 Cloudflare Workers Secret과 GitHub Secrets에만 존재한다. 커밋 전 `git diff`에서 키 문자열을 확인한다. |
| A4 | **집 주소·좌표·정류장 ID·휴가 일정을 코드에 하드코딩하지 않는다.** 저장소는 public일 수 있다. 전부 `localStorage` 설정값이며, `config.default.json`에는 **더미값**만 넣는다. |
| A5 | **시각 계산에 `new Date()`의 로컬 시간대를 신뢰하지 않는다.** 모든 시각은 `Asia/Seoul` 기준. §6 참조. |
| A6 | **영업일 게이트를 우회하는 코드를 만들지 않는다.** 알림을 보내는 모든 경로는 `isBusinessDay()`를 먼저 통과해야 한다. §5 참조. |
| A7 | **계산 로직을 PWA용/Actions용으로 두 벌 만들지 않는다.** `planner.js`, `businessday.js`, `weather.js`는 양쪽이 공유하는 단일 소스다. |

---

## 3\. 기술 스택 (확정, 변경 시 협의)

- **프런트**: Next.js (App Router, JavaScript, `output: 'export'` 정적 빌드). 스타일은 CSS Modules.  
- **호스팅**: GitHub Pages (`next build`가 생성한 `out/`을 배포). 저장소명에 따른 `basePath` 설정은 §14 참고.  
- **프록시**: Cloudflare Workers 무료 플랜 — CORS 우회 \+ API 키 은닉 \+ 캐시. 그 이상의 역할을 주지 않는다(비즈니스 로직 금지).  
- **스케줄러/알림**: GitHub Actions cron → Telegram Bot API.  
- **저장소**: `localStorage` 만. 외부 DB·계정·로그인 없음.  
- **Node**: Next.js 빌드/개발 서버 및 Actions 스크립트에서 사용. 버전은 `package.json`(`engines`)과 워크플로에 고정한다.

---

## 4\. 파일 구조와 책임 경계

gonow/ (저장소 루트)

├─ src/

│   ├─ app/

│   │   ├─ layout.js          \# 루트 레이아웃. 전역 스타일 import만

│   │   ├─ page.js            \# 홈 화면. 계산 금지, lib에서 받은 값만 렌더

│   │   ├─ globals.css

│   │   └─ settings/page.js   \# 설정 화면

│   ├─ components/            \# 순수 렌더링 컴포넌트. 계산 금지

│   └─ lib/

│       ├─ planner.js         \# 역산 엔진. 순수 함수. DOM/fetch 금지

│       ├─ businessday.js     \# 영업일 판정. 순수 함수. DOM/fetch 금지

│       ├─ weather.js         \# 기상·대기질 해석 \+ 옷차림/우산 룰. 순수 함수

│       └─ sources.js         \# 외부 API 호출 어댑터. 여기서만 fetch 한다

├─ data/

│   ├─ gtx-a.json        \# GTX 시간표 (손으로 갱신)

│   ├─ holidays.json     \# 공휴일 (Actions가 월 1회 갱신)

│   └─ wardrobe.json     \# 기온 구간별 옷차림 룰

├─ public/

│   ├─ config.default.json   \# 더미 기본값만

│   └─ manifest.json, sw.js

├─ next.config.mjs       \# `output: 'export'` 고정

├─ .github/workflows/

│   ├─ briefing.yml      \# 영업일 아침 브리핑

│   ├─ reminder.yml      \# 출발 리마인더

│   └─ sync-holidays.yml \# 공휴일 동기화

├─ worker/proxy.js       \# Cloudflare Workers

├─ scripts/              \# Actions에서 실행하는 Node 진입점 (src/lib을 상대 경로로 import)

└─ PRD.md

**경계 규칙**

- `src/lib/planner.js` / `businessday.js` / `weather.js`는 **순수 함수만** 둔다. 입력을 인자로 받고 결과를 반환한다. 내부에서 `fetch`, `Date.now()`, `localStorage`, `document`를 **직접 호출하지 않는다.** → 현재 시각도 인자로 받는다: `plan({ now, timetable, busArrivals, weather, config })` → 이 규칙 덕분에 시각을 고정해 테스트할 수 있다. 이걸 깨면 검증이 불가능해진다. Next.js 프런트와 Actions(Node) 양쪽에서 같은 파일을 그대로 import한다(PWA/Actions 이중 구현 금지, §2 A7).  
- `src/lib/sources.js`가 유일한 네트워크 경계다. 외부 API 응답 형태(이상한 필드명, 문자열 숫자, 코드값)는 여기서 전부 정규화해서 내보낸다. **바깥으로 원본 응답을 그대로 흘리지 않는다.**  
- `src/app/**/page.js`와 `src/components/`는 계산하지 않는다. `src/lib`에서 받아온 값을 그리기만 한다. `"use client"`는 상태·이벤트가 실제로 필요한 컴포넌트에만 붙인다.

---

## 5\. 영업일 게이트 (도메인 규칙 1\)

**모든 능동 동작은 게이트를 먼저 통과한다.** 이것은 기능이 아니라 선행 조건이다.

isBusinessDay(date, holidays, overrides)

// → { business: boolean, mode: "WORK"|"REMOTE"|"VACATION"|"WEEKEND"|"HOLIDAY", reason: string }

판정 순서(앞이 우선):

1. 사용자 수동 지정(`overrides`) — **항상 최우선**  
2. 토·일요일  
3. `holidays.json` (대체·임시공휴일 포함)  
4. 그 외 → 영업일

지켜야 할 것:

- 알림 워크플로의 **첫 스텝**이 게이트다. 비영업일이면 `exit 0`으로 **조용히** 종료한다. 에러로 처리하거나 "오늘은 휴일입니다" 알림을 보내지 않는다.  
- `mode === "REMOTE"`는 영업일이지만 교통 계산을 하지 않는다. 날씨만 보낸다.  
- **비영업일에도 사용자가 직접 앱을 열면 교통 정보는 정상 조회된다.** 막는 것은 *알림*이지 *조회*가 아니다.  
- cron은 월\~금으로 걸되, 공휴일 판정은 반드시 코드에서 한다. cron은 공휴일을 모른다.

---

## 6\. 시간 처리 (도메인 규칙 2 — 가장 사고가 잦은 곳)

- **GitHub Actions 러너는 UTC로 돈다.** cron을 한국시간으로 쓰면 9시간 어긋난다. 한국시간 07:05 \= `5 22 * * 0-4` (요일도 하루 밀린다). 워크플로 cron 옆에 반드시 주석으로 KST 환산을 적는다.  
- 코드 안에서는 `Asia/Seoul`을 명시한다. 러너·브라우저의 로컬 시간대에 의존하지 않는다.  
  // 권장: 유틸 하나로 통일  
  const KST \= 'Asia/Seoul';  
  function nowKST() { /\* Intl.DateTimeFormat(KST) 기반 \*/ }  
- 시각 비교는 문자열 `"HH:MM"`이 아니라 **자정 기준 분(minutes-from-midnight) 정수**로 한다. 문자열 비교는 `"9:05" < "10:00"`에서 틀린다.  
- 자정을 넘기는 시간표(막차 `24:30` 등)를 전제한다. 시간표 JSON은 `24:30` 표기를 허용하고 파서가 흡수한다.

---

## 7\. 역산 엔진 (도메인 규칙 3\)

- **평균 소요시간을 더하지 않는다.** 실제로 탈 수 있는 **특정 편**을 고정한다. 도보 이후 도착 가능한 가장 이른 GTX 편 → 그 도착시각 이후 탈 수 있는 가장 이른 버스. 목표 도착시각을 넘기면 한 편 앞으로 당긴다.  
- 버퍼·도보시간·날씨 보정계수는 **전부 설정값**이다. 함수 안에 숫자를 박지 않는다. 매직넘버가 보이면 설정으로 빼낸다.  
- 날씨는 표시용이자 **역산의 입력 변수**다. 비 오면 도보 \+20%가 출발시각을 실제로 앞당겨야 한다.  
- 판정 결과에는 항상 `reasons` 배열을 채운다. 왜 그 시각인지 사용자가 납득할 수 있어야 한다.

---

## 8\. 장애 처리 원칙

- **부분 실패는 전체 실패가 아니다.** 날씨 API가 죽어도 교통 판정은 나와야 하고, 버스 API가 죽어도 GTX 시간표 기반 추정은 나와야 한다.  
- **추정값을 사실처럼 보여주지 않는다.** 실시간 데이터가 없으면 화면과 메시지에 `(시간표 기준 추정)`을 명시한다. 조용히 폴백해서 사용자를 속이는 것이 가장 나쁜 실패다.  
- 알림은 지연될 수 있다(Actions cron은 정시 보장이 없다). 그래서 출발시각은 점이 아니라 **창(window)** 으로 표현하고, 메시지에 실제 발송시각을 적는다.  
- API 응답이 비었을 때 `undefined`가 화면까지 흘러가지 않게 `sources.js`에서 막는다.

---

## 9\. 코딩 컨벤션

- 들여쓰기 2칸, 세미콜론 사용, 작은따옴표.  
- 함수·변수는 영문 camelCase. **주석과 커밋 메시지, 사용자에게 보이는 문자열은 한국어.**  
- 함수 하나는 한 가지만 한다. 40줄을 넘으면 쪼갤 자리를 찾는다.  
- 조건문에 숫자가 그대로 등장하면 이름을 붙이거나 설정으로 뺀다.  
- `try/catch`로 에러를 삼키지 않는다. 잡았으면 로그를 남기고 사용자에게 보일 상태로 바꾼다.  
- 주석은 "무엇"이 아니라 **"왜"** 를 적는다. 특히 매직넘버·우회책·API 특이사항.  
- DOM 조작은 React 컴포넌트(`src/app/`, `src/components/`)에 모은다. `dangerouslySetInnerHTML`에 외부 데이터를 그대로 넣지 않는다.

---

## 10\. 검증 방법 (테스트 프레임워크 없이)

- `scripts/check-*.mjs` 형태의 Node 스크립트로 순수 함수를 검증한다. 프레임워크 설치 금지.  
- **반드시 고정 입력으로 검증한다.** `now`를 인자로 받는 설계(§4)를 이용해 특정 시각·특정 시간표·특정 버스 응답을 넣고 기대 결과와 비교한다.  
- 최소한 다음 케이스는 항상 통과해야 한다:  
  - 평일 07:30 정상 케이스  
  - 토요일 / 공휴일 / 휴가 등록일 → 게이트 차단  
  - 공휴일이지만 `WORK` 오버라이드 → 통과  
  - 막차 이후 / 첫차 이전  
  - 버스 API 빈 응답 → 추정 모드로 폴백하고 그 사실이 표시됨  
  - 비 예보 시 출발시각이 실제로 앞당겨짐  
  - UTC 환경에서 실행해도 KST 기준 결과가 동일함 (`TZ=UTC node ...`로 확인)  
- 화면 변경은 **휴대폰 폭(약 390px)** 에서 확인한다. 데스크톱만 보고 끝내지 않는다.

---

## 11\. 작업 방식

- **한 번에 하나씩.** 요청받지 않은 리팩터링·파일 이동·이름 변경을 함께 하지 않는다. 필요해 보이면 먼저 제안하고 동의를 받는다.  
- 기존 동작을 바꾸는 변경은 **바꾸기 전에 무엇이 왜 바뀌는지** 한국어로 한두 줄 설명한다.  
- 파일을 새로 만들기 전에 기존 파일에 들어갈 자리가 없는지 먼저 본다. 파일 수를 늘리는 것을 기본값으로 삼지 않는다.  
- README·요약 문서를 요청 없이 만들지 않는다.  
- 모르면 추측해서 채우지 말고 **묻는다.** 특히 정류장 ID, 도보 실측 시간, 기상 격자좌표처럼 **사실 확인이 필요한 값은 임의로 채워 넣지 않는다.** 자리표시자를 넣었으면 `TODO(확인필요):` 로 표시한다.  
- 커밋은 작게, 메시지는 한국어 한 줄 \+ 필요 시 본문. 예: `영업일 게이트에 재택 모드 추가`

---

## 12\. 이 프로젝트에서 이미 알고 있는 함정

> 같은 실수를 반복하지 않기 위한 목록. 새로 발견하면 여기에 추가한다.

1. **한국 공공데이터 API는 CORS 헤더를 주지 않는다.** 브라우저에서 직접 부르면 막힌다. 반드시 Workers 프록시를 경유한다. "배포하면 될 것"이라는 기대는 틀렸다.  
2. **GitHub Actions cron은 UTC이며 정시 보장이 없다.** 혼잡 시 수 분\~15분 지연된다. 선행 실행 \+ 출발 창 표현으로 흡수한다.  
3. **GTX-A는 공개 실시간 API가 없다.** 정적 시간표 기반이며 지연은 반영되지 않는다. "실시간 GTX 정보"를 구현했다고 말하지 않는다. 비공식 스크래핑은 쓰지 않는다.  
4. **기상청 API는 초단기실황(현재값)과 단기예보(최저·최고·강수확률)가 다른 오퍼레이션이다.** 하나로 다 되지 않는다. 격자 좌표(nx, ny)를 쓰며 위경도가 아니다. 발표 시각 전에는 직전 회차로 폴백해야 한다.  
5. **공공 API 응답은 숫자를 문자열로, 결측을 특수코드로 준다.** `sources.js`에서 정규화하지 않으면 계산이 조용히 틀어진다.  
6. **API 일일 호출 쿼터가 있다(개발계정 기준 1,000건/일 가정).** 무제한 폴링 금지. 통근 시간대에만, 캐시와 함께.  
7. **옷차림은 아침 기온만으로 판단하면 틀린다.** 퇴근 시각 기온까지 보고 더 낮은 쪽을 기준으로 한다.  
8. **브리핑 메시지가 길어지면 안 읽힌다.** 비가 안 오면 우산 줄을 아예 빼고, 미세먼지가 '보통' 이하면 생략한다. 조건부 노출이 기본이다.

---

## 13\. 용어

| 용어 | 뜻 |
| :---- | :---- |
| **출발각 / verdict** | 이 앱의 유일한 결론. `GO / WAIT / LATE / IDLE / OFF_DAY` |
| **영업일 게이트** | 능동 동작 전에 통과해야 하는 선행 조건 (§5) |
| **역산** | 목표 도착시각에서 거꾸로 계산해 출발시각을 구하는 것 (§7) |
| **출발 창(window)** | 점 하나가 아닌 `07:35~07:41` 형태의 권장 출발 구간 |
| **d1\~d4** | 순서대로 집→역 / 개찰→승강장 / 서울역→정류장 / 정류장→회사 도보 |
| **모드(mode)** | `WORK` / `REMOTE` / `VACATION` / `WEEKEND` / `HOLIDAY` |

---

## 14\. 확정된 값 / 아직 확정되지 않은 것

**확정됨 (2026-09-13, 임의로 바꾸지 말 것)**

- 목표 도착시각: **08:20**  
- 도보 시간: **d1 10분 · d2 5분 · d3 7분 · d4 5분**  
- 출근길 정류장: 서울역 승차 **"서울역 6번 승강장"**(정류소번호 **02006**) → 하차 **"우리은행종로지점"**(정류소번호 **02139**, "종로2가.우리은행"이라고도 부름)  
- 출근길 버스 노선: **103 · 173 · 202 · 261 · 262 · 7017 · 7021** (이 방향. 반대 방향인 603·701·702A·702B 등과 혼동 금지)  
- **퇴근 경로는 출근의 역순이 아니다.** 교원빌딩 → 도보 5분 → 롯데백화점 본점(정류소번호 **02140**) → 버스 → 서울역.  
- 퇴근길 버스 노선: **100 · 103 · 162 · 173 · 201 · 261 · 262 · 500 · 501 · 502 · 506 · 7017 · 7021**  
- 퇴근길 하차 지점은 **버스 노선마다 다르다** — 숭례문 / 서울역버스환승센터 4번 승강장(정류소번호 **02004**) / 염천교 중 하나. `planner.js`/`sources.js`는 하차 지점을 **노선별로** 매핑해야 한다(전체 노선에 고정된 하나의 하차 정류장ID를 쓰면 안 됨). 숭례문·염천교는 이름이 같은 정류장이 여러 개라 노선별 정확한 정류장ID는 구현 시점에 노선별로 확인 필요 (아래 참고)  
- 기상청 격자좌표: 운정 **nx55·ny130**, 명동 **nx60·ny127** (운정중앙역·명동역 좌표를 LCC 변환 공식에 넣어 계산)  
- 회사 영업일 = 법정공휴일과 일치 (별도 예외일 등록 불필요)  
- 재택근무: **없음** (REMOTE 모드는 스키마상으로만 유지)  
- 저장소: **public**, GitHub 저장소 이름 **`workspace1`** (이미 생성·푸시됨). GitHub Pages 프로젝트 페이지로 배포 시 `next.config.mjs`에 `basePath: '/workspace1'` 지정 필요 (아직 미적용).  
- 브리핑 발송 시각: **07:00**

**아직 확정되지 않은 것 (임의로 정하지 말 것)**

- 퇴근길 **노선별 하차 정류장ID** — 숭례문·염천교는 동명 정류장이 여러 개라 노선(100/103/162/173/201/261/262/500/501/502/506/7017/7021) 각각 실제로 어느 정류장에 서는지 확인 필요. (4번 승강장=02004는 확정)  
- 서울역~명동 도보 대체 가능 여부, GTX 지연 시 대안 경로, 옷차림 판정 기준(체감온도 vs 실제기온) — PRD.md §14 참고.

&nbsp;
